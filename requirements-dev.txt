# Dev/test dependencies (installed on top of requirements.txt).
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                    # FastAPI TestClient transport + load test
ruff>=0.5                     # lint + format (PEP 8)
mypy>=1.10                    # optional static type checking
