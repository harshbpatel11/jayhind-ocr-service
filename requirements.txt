# LLM invoice-OCR service — Python dependencies.
#
# torch + CUDA are PRE-INSTALLED on Kaggle / Colab / most GPU-provider images, so
# they are intentionally NOT pinned here. PaddlePaddle itself is installed by
# run.sh from the Paddle CUDA index (it is not a normal PyPI package).

paddleocr>=3.2.0          # PaddleOCR-VL pipeline (the reader)
transformers>=4.45        # extractor LLM (Qwen2.5 by default)
accelerate>=0.34
bitsandbytes>=0.43        # 4-bit loading for the extractor LLM

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # multipart file upload
pillow>=10.0              # image decode
pymupdf>=1.24             # PDF page rasterisation
requests>=2.31
pyngrok>=7.0              # optional ngrok tunnel (Cloudflare needs no package)
