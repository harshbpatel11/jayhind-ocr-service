# Invoice Scanning — OCR sidecar service
#
# Core (always required)
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pdfplumber==0.11.5          # digital-PDF text layer + word boxes
pymupdf==1.25.2             # page rasterisation for scanned PDFs
pillow==11.1.0

# OCR engine (optional at import time — the digital-PDF path works without it).
# CPU build. See README.md for GPU (paddlepaddle-gpu) instructions.
#
# NOTE: PaddleOCR 3.x's default model (PP-OCRv6) segfaults on aarch64/ARM, and
# multi-threaded CPU inference does too. `app/config.py` therefore defaults to
# PP-OCRv5 mobile with cpu_threads=1 — both overridable by env var.
paddlepaddle==3.2.2
paddleocr==3.7.0

# OPTIONAL — PaddleOCR-VL local vision-language OCR engine (OCR_ENGINE=vl).
# Adds the VLM backend; weights (~1.8 GB) download on first use. Install with:
#     pip install "paddlex[ocr]==3.7.2"
# ⚠️ Segfaults on aarch64/CPU inference — x86 / GPU hosts only. The default engine
# is "classic" (PP-OCRv5), so this is NOT required for normal operation.
